# Automation Issue Context: Monitor lime-ime/limeime GitHub

Last updated: 2026-05-26T16:47:14+08:00 (CST)

## Source Of Rules
- Canonical role, communication style, issue-tracking policy, and APK/build policy are stored in the local automation memory file:
  `C:\Users\Jeremy\.codex\automations\monitor-lime-ime-limeime-github\memory.md`
- This repo file is for mutable issue-tracking context, operational handoffs, and shared task-board items only, so scheduled runs and different agent identities can read/update current state through GitHub API.

## Current Observed Repo State
- Latest GitHub Release observed: `v6.1.15` (published 2026-05-26) at https://github.com/lime-ime/limeime/releases/tag/v6.1.15, with release asset `LIMEHD2026-6.1.15.apk`; download link https://github.com/lime-ime/limeime/releases/download/v6.1.15/LIMEHD2026-6.1.15.apk. Previous formal release was `v6.1.12` (published 2026-05-25); 6.1-series baseline before that was `v6.0.2` (published 2026-04-23).
- Android pre-release/build artifacts are not necessarily public GitHub Releases; as of v6.1.15, the tracked Android APK is also attached to the official GitHub Release.
- Scheduled/API-only APK source of truth: `LimeStudio/app/release/output-metadata.json`.
- Last observed pre-release APK: `LIMEHD2026-6.1.15.apk` (versionName 6.1.15), metadata observed 2026-05-26T15:40+08:00 after commit `d0eb4c047aac`. Direct raw APK link `https://raw.githubusercontent.com/lime-ime/limeime/master/LimeStudio/app/release/LIMEHD2026-6.1.15.apk`. This APK contains the #88 legacy v5.2.4/v6.0 restore/import emoji FTS fallback fix (`emoji_fts already exists`) and retains prior v6.1.14 Samsung/One UI IME settings activity metadata fix plus v6.1.13 Samsung/Android 13 settings scrollbar fix. #88 reporter `peter8777555` confirmed v6.1.15 is fully normal in https://github.com/lime-ime/limeime/issues/88#issuecomment-4541845116, closed the issue, and automation acknowledged in https://github.com/lime-ime/limeime/issues/88#issuecomment-4541914188. The previous tracked APK was `LIMEHD2026-6.1.14.apk` / version 6.1.14.
- Raw APK URL pattern: `https://raw.githubusercontent.com/lime-ime/limeime/master/LimeStudio/app/release/<apk filename>`.
- No open pull requests were observed in the 2026-05-22T04:18:00+08:00 scheduled run.
- No GitHub Discussions tab was observed for the repository in recent runs.

## Current Issue States And Required Actions
- #54: Community-reported Brave URL bar candidate overlap/white-band behavior. Relevant fix landed in APK `LIMEHD2026-6.1.5.apk`; a retest request was posted and the issue is closed as completed. Remove from active watch unless reopened or referenced by new reports.
- #55: Community-reported key preview delay. Reporter confirmed improvement on 6.1.1 pre-release. Closed as completed; remove from active watch unless reopened or referenced by new reports.
- #58: `.lime` pipe-format import/support question. Reporter `ejmoog` confirmed in comment `4526128600` (`https://github.com/lime-ime/limeime/issues/58#issuecomment-4526128600`) that `lime-text-v2` on version 6.1.9 can import escaped `\`, `|`, `@`, and `%`, then closed the issue on 2026-05-23. Hermes/limeimetw added a `+1` reaction and live closing acknowledgement `4526135538` (`https://github.com/lime-ime/limeime/issues/58#issuecomment-4526135538`); duplicate/stale acknowledgement references from concurrent closure runs were removed/ignored. Treat as resolved/closed documentation question; remove from active watch unless reopened or new import-failure evidence appears.
- #59: `.lime` format/name-display documentation question. Maintainer answered key format/name details, reporter thanked them, and `jrywu` closed the issue on 2026-05-21. Treat as resolved/closed documentation question; no active bug watch or follow-up unless the reporter reopens or adds new evidence/question.
- #62: Community-reported Ext-B leading-character related-phrase issue. Reporter confirmed the 6.1.6 retest fixed/improved the remaining path; automation reacted and closed as completed. Remove from active watch unless reopened or referenced by new reports.
- #63: Community-reported Google voice input outputs Simplified Chinese and previously duplicated text. Reporter tested 6.1.6 after the voice-input routing/inline-dictation fixes and said it still outputs Simplified Chinese both with microphone permission enabled and disabled; clarification in comment `4501864655` says the normal Traditional Chinese output is from Sweetlime. Maintainer `jrywu` explained in comment `4509767537` that LIME already requests `zh-TW` and the system recognizer still returns Simplified Chinese on the reporter's Xiaomi setup; SweetLime uses legacy VoiceIME behavior that newer target API builds cannot reliably invoke. `jrywu` closed the issue on 2026-05-21 as effectively an upstream Xiaomi/Android recognizer limitation after LIME-side mitigations. Reporter follow-up in comment `4509924146` thanks the team and clarifies this is one Xiaomi phone with both LIME 6.1.6 and SweetLime installed, not two separate Xiaomi devices; no reopening request or new actionable LIME-side evidence yet. Treat as maintainer-closed; do not include #63 in routine reminder/watch summaries or shared task reminders unless the reporter reopens/adds new evidence, a clearly relevant platform workaround lands, or Jeremy explicitly asks to resume.
- #64: Community-reported settings text/inset/scroll issue. Reporter confirmed `6.1.2` fixed it; closed as completed. Remove from active watch unless reopened.
- #65: Maintainer-created Android table/assoc editor soft-keyboard sheet issue. Relevant fix landed in 6.1.4. Closed directly as completed; no tester invitation needed.
- #66: Maintainer-created iOS assoc/related editor score-field issue. Relevant fix landed in 6.1.4. Closed directly as completed; no tester invitation needed.
- #67: Community-reported 6.1.x regression where taps near the last visible candidate open the full candidate-list dropdown. Reporter confirmed fixed on 6.1.7 in comment `4500383587`; automation reacted/closed as completed in comment `4502010433`. Remove from active watch unless reopened.
- #68: Maintainer-created cross-platform candidate-bar dismiss bug. Relevant fix landed in 6.1.7; issue is closed as completed. Remove from active watch unless reopened.
- #69: Maintainer-created cross-platform candidate-bar tool-icon flicker bug. Relevant fix landed in 6.1.7; issue is closed as completed. Remove from active watch unless reopened.
- #70: Community request to hide or configure next-code/next-character candidates. Reporter clarified it is not a physical keyboard issue and that the requested setting is the number of `延伸編碼` candidates, where `0` would mean disabled. Maintainer `jrywu` replied in comment `4506632626` that this should be the suggested-candidate display-count option and later closed the issue on 2026-05-21T14:16:19Z. Treat as maintainer-resolved/closed for now; remove from active watch unless the reporter reopens or adds follow-up indicating the option does not solve it.
- #71: Community-reported Chinese/English switch composition-cancel bug. Reporter confirmed Android APK `LIMEHD2026-6.1.8.apk` implements the requested behavior in comment `4514378812` (`https://github.com/lime-ime/limeime/issues/71#issuecomment-4514378812`) and closed the issue as completed on 2026-05-22. Automation added a thumbs-up reaction and short closing acknowledgement, and `docs/#71_ISSUE.md` now marks reporter-confirmed fixed. Treat as resolved/closed; remove from active watch unless reopened or referenced by new reports.
- #72: Community request to add 哈哈倉頡 as a downloadable IM and clarify current Cangjie table source/license. Labeled `enhancement` and `question`; reporter `ejmoog` confirmed in comment `4532335292` (`https://github.com/lime-ime/limeime/issues/72#issuecomment-4532335292`) that Android APK `LIMEHD2026-6.1.12.apk` includes 哈哈倉頡. Automation added a `+1` reaction, closed the issue as completed, and posted closing acknowledgement `https://github.com/lime-ime/limeime/issues/72#issuecomment-4532375300`. Treat as resolved/closed for the Android downloadable table/catalog scope; remove from active watch unless reopened or new candidate/phrase/punctuation mismatch evidence appears. iOS catalog/source work is on `master`, but iOS/TestFlight delivery remains separate.
- #73: Community request: make Android soft-keyboard Enter commit the raw English/code string while Space continues selecting/committing Chinese candidates, example `bgg` -> `bgg` on Enter. Reporter is on APK 6.1.7 / Android 15 and cites Trime. Classified as `enhancement` + `Usability`; automation acknowledged in comment `4504038619`. Maintainer `jrywu` replied in comment `4509178653` that LIME's Space/Enter commit behavior can already move the preselection to the English code when no direct Chinese match exists, that the suggestion may be considered but needs scheduling, and closed the issue on 2026-05-21. Reporter `ejmoog` followed up in comment `4517972425` (`https://github.com/lime-ime/limeime/issues/73#issuecomment-4517972425`) that even if collisions are uncommon, Enter and Space still feel functionally duplicated. Treat as maintainer-closed/low-priority product consideration for now; no bug investigation doc, public duplicate reply, or active watch is needed unless Jeremy/maintainer wants to reopen/product-plan it or the reporter adds new actionable evidence.
- #74: Community question/enhancement about Android `記憶中英模式` and inputType-driven switching. Reporter confirmed APK `6.1.10` fixed numeric/decimal/phone fields using restricted numeric keyboards, and later confirmed APK `6.1.11` makes the tested URL field follow remembered Chinese/English mode like normal text fields; search-field coverage follows the same build scope but was not separately reporter-tested. After maintainer clarification, reporter `SmithCCho` said the issue can be closed in https://github.com/lime-ime/limeime/issues/74#issuecomment-4527392181; a +1 reaction was added, `limeimetw` posted closing acknowledgement https://github.com/lime-ime/limeime/issues/74#issuecomment-4527405069, and the issue is closed as completed. Treat as reporter-confirmed resolved for the scoped Android behavior; remove from active watch unless reopened or new evidence appears. `docs/#74_ISSUE.md` records the reporter-confirmed resolution and verified scope.

- #75: Community-reported Android keyboard redraw/popup-lifecycle bugs on 6.1.7 / Android 15. Fix commit `9230916` landed and APK `LIMEHD2026-6.1.8.apk` contains the relevant keyboard popup/redraw changes. Retest request was posted in `https://github.com/lime-ime/limeime/issues/75#issuecomment-4510656707`; reporter `ejmoog` confirmed in `https://github.com/lime-ime/limeime/issues/75#issuecomment-4514371698` that installing 6.1.8 resolved the issue, then closed it. Automation added a thumbs-up reaction and a short closing acknowledgement. Treat as resolved/closed; remove from active watch unless reopened or referenced by new reports.
- #76: Community-reported Android next-code/extension candidate display bug split from #70 after reporter tested APK `LIMEHD2026-6.1.8.apk` and said the extra candidate still appeared. Fix commit `7e1d57b` is on `master`; Android APK `LIMEHD2026-6.1.9.apk` contained the targeted `建議字顯示數量 = 0` / exact-match-only fix. Reporter `ejmoog` confirmed in `https://github.com/lime-ime/limeime/issues/76#issuecomment-4520021201` that the scoped problem was resolved, then closed the issue; Hermes acknowledged in `https://github.com/lime-ime/limeime/issues/76#issuecomment-4520032322`. Later commit `5d66014dead7` in APK `LIMEHD2026-6.1.10.apk` fixes the adjacent `learn_phrase=false` ungated runtime phrase / LD phrase learning paths on Android and iOS, but #76 remains reporter-closed for the scoped exact-match issue. Do not post another #76 retest prompt or reopen unless the reporter/maintainer asks or new evidence appears; if learned-word/association/ranking-control concerns resurface, distinguish existing learned/table entries, relation candidates, and selection-ranking data before deciding whether to split or triage separately.


- #77: Maintainer-created iOS second-stage candidate placeholder bug. Maintainer `jrywu` posted fix summary in comment `4517540556` and closed the issue on 2026-05-22 after local simulator verification; commit `c828a2d2` is referenced by GitHub. Analysis doc `docs/#77_ISSUE.md` now records the closed/fixed status. Treat as resolved/closed; no public acknowledgement, reporter retest request, or active watch is needed unless new iOS evidence appears.
- #79: Community-reported Android dark-mode emoji panel search-field color bug from `SmithCCho`. Live labels are `bug` + `Usability`, issue is open and assigned to `jrywu`; public acknowledgement already exists in `https://github.com/lime-ime/limeime/issues/79#issuecomment-4523853744`. Commit `a763ee80b199` and Android pre-release APK `LIMEHD2026-6.1.12.apk` contain the scoped emoji search-field/theme fix; Hermes posted retest request `https://github.com/lime-ime/limeime/issues/79#issuecomment-4529659072`. Reporter confirmed in `https://github.com/lime-ime/limeime/issues/79#issuecomment-4531664505` that the Android dark-mode search-field background and icon are no longer too bright. The same comment raised a separate emoji-search input behavior question: 行列/倉頡 code input such as `100` is not entered into the emoji search field and instead goes to the host app. Code inspection shows English/ASCII direct key consumption and Chinese IM composition/candidate handling use different paths; after maintainer clarification, treat the reporter's Chinese emoji-search concern as an adjacent 6.1.12 follow-up pending reporter confirmation, not a failed visual fix. Recommended next step: maintainer can close #79 for the confirmed Android visual/theme fix once comfortable, or leave it open briefly for SmithCCho to confirm the 6.1.12 Chinese emoji-search clarification. The same push includes iOS emoji/search source work, but Android APK confirmation does not verify iOS/TestFlight behavior. Later comments add context: `Limeroshenko` reported normal APK 6.1.12 behavior on Pixel 9 / Android 16 (`https://github.com/lime-ime/limeime/issues/79#issuecomment-4531768150`), while `SmithCCho` clarified their test environment as APK 6.1.12 on Samsung A16 / Android 16 / One UI 8.0 (`https://github.com/lime-ime/limeime/issues/79#issuecomment-4531782542`). Do not overread the Pixel comparison as resolving the Chinese-IM-code input question; jrywu's later clarification is the public maintainer answer for now. Avoid duplicate public acknowledgement unless the reporter adds new evidence after that clarification. Maintainer `jrywu` then clarified publicly in `https://github.com/lime-ime/limeime/issues/79#issuecomment-4531795839` that APK 6.1.12 supports Chinese emoji search and 6.1.11 and earlier did not. Current next step: no automation public reply needed; wait for reporter follow-up or maintainer closure decision.
- #80: Community question from `ejmoog` asking whether `.cin` `%version` has practical effect or can be omitted. Classified as `question` + `documentation`; Hermes verified `docs/CIN_LIME_SPEC.md`, `docs/IM_VERSION.md`, Android `LimeDB.java`, and iOS `LimeDB.swift`: `%version` is intended to persist as `im.title = "version"`, show in IM detail, and be preferred during export; if omitted, imports still work with `%cname`/filename fallback. Hermes replied in `https://github.com/lime-ime/limeime/issues/80#issuecomment-4523884786`, then posted follow-up `https://github.com/lime-ime/limeime/issues/80#issuecomment-4523897594` after the reporter showed filename fallback, recommending `.cin` extension + remove/re-import. Reporter confirmed in `https://github.com/lime-ime/limeime/issues/80#issuecomment-4523915713` that changing the filename to `.cin` solved the `%version` display path and closed the issue on 2026-05-23; Hermes acknowledged in `https://github.com/lime-ime/limeime/issues/80#issuecomment-4523920587`. Immediately after closure, reporter asked in `https://github.com/lime-ime/limeime/issues/80#issuecomment-4523923704` why the same document has fewer entries after renaming to `.cin`; Hermes answered in `https://github.com/lime-ime/limeime/issues/80#issuecomment-4523926632` that `.cin` uses stricter CIN parsing (`%chardef begin/end`, code + space/Tab + word) while non-`.cin` files use auto delimiter detection, and asked for metadata plus sample rows if they want format diagnosis. Treat as a closed documentation/import-format question; no bug analysis doc, retest, or active fix watch is needed unless the reporter provides a sample showing current `.cin` import drops valid CIN rows.



- #85: Maintainer-created Android+iOS database backup restore bug. Live issue is open, labeled `bug`, assigned to `jrywu`; analysis doc `docs/#85_ISSUE.md` exists and includes both platforms. Android restore failure propagation/invalid ZIP handling was merged through PR #87 as `d22afcfddc82c0fc1260a5a09789590778199ec1` on 2026-05-25, follow-up commit `f80ce0cb8884` restored compatibility with legacy Android backup archives that contain leading-slash entries, and the same-version Android APK `LIMEHD2026-6.1.12.apk` was rebuilt in commit `e5b06c79d22c` during push `8d6a33e93b226` -> `c97ee41f6ee2`; that APK is now also attached to official GitHub Release `v6.1.12`. Keep #85 open unless/until the maintainer decides Android is complete and the remaining iOS restore failure propagation/validation work is split/covered by #86 or another issue. No public acknowledgement or community retest request is needed because this is maintainer-created.

- #86: Maintainer-created iOS database restore state-sync bug. Live issue is open, labeled `bug`, assigned to `jrywu`; analysis doc `docs/#86_ISSUE.md` records that LIME Settings shows restored IM tables after a successful restore, but the keyboard extension can still show zero IMs until the user removes/re-adds the LIME system keyboard. Likely fix area: iOS restore-to-keyboard handoff in `SetupImController.restoreDB(from:)`, app-group restore signal `lime_db_restored_at`, `KeyboardViewController.setupDatabase()`, and `DBServer.prepareKeyboardRuntimeDatabase()` validation/regeneration of `keyboard_state` / `keyboard_list` against the restored IM table. No public acknowledgement or community retest request is needed because this is maintainer-created.

- #88: Community-reported Android startup/settings crash on Samsung A71 4G / Android 13, with later legacy v5.2.4/v6.0 restore/import crash evidence. Earlier phases: v6.1.13 addressed the Samsung scrollbar crash path; PR #89 and APK `LIMEHD2026-6.1.14.apk` addressed the stale Samsung/One UI `android:settingsActivity` / `MainActivity` path, and the reporter replied that v6.1.14 could enter the app. The remaining concrete failure was restoring/importing preserved v5.2.4/v6.0-era settings/backups: reporter logs from https://github.com/lime-ime/limeime/issues/88#issuecomment-4541068761 showed LIME `AndroidRuntime` crashes in `Process: net.toload.main.hd2026` with `android.database.sqlite.SQLiteException: table emoji_fts already exists` while falling back from FTS5 to FTS4 during `LimeDB OnUpgrade() db old version = 101, new version = 104`; reporter confirmed in https://github.com/lime-ime/limeime/issues/88#issuecomment-4541246237 that they still have v5.2.4/v6.0.0 backups and that v6.0.2 can import the v5.2.4 backup normally. Commit `d0eb4c047aac799b28dde6f9a8643790aa0103f6` built Android pre-release APK `LIMEHD2026-6.1.15.apk` / version `6.1.15` with the targeted emoji FTS fallback cleanup and regression test; Hermes reopened the community issue and posted the scoped v6.1.15 retest request at https://github.com/lime-ime/limeime/issues/88#issuecomment-4541715912 after GitHub auto-closed it from the fixing commit. Reporter `peter8777555` then confirmed in https://github.com/lime-ime/limeime/issues/88#issuecomment-4541845116 that `v6.1.15 全部正常 已正常執行無誤` and closed the issue. Automation added a `+1` reaction and posted closing acknowledgement https://github.com/lime-ime/limeime/issues/88#issuecomment-4541914188. Current state: reporter-confirmed resolved/closed; remove from active watch unless reopened or new failed evidence appears. Later same-issue evidence from `ejmoog` in https://github.com/lime-ime/limeime/issues/88#issuecomment-4541728673 says Samsung A52 5G / Android 15 has verified LIME v6.1.13 can be used normally; treat that as a compatibility datapoint only, with no public reply needed unless new failed evidence appears.

- #90: Community enhancement/usability request from `kenny72014a` asking for keyboard-interface customization: hide the English/number switch key, optionally use an old-style phonetic keyboard, and choose whether Emoji/voice buttons appear to reduce accidental taps and simplify the UI. Classified as `enhancement` + `Usability`; no bug analysis doc needed. Hermes checked current settings/docs and Android layout/candidate-bar docs: current visible settings include keyboard appearance/behavior options, `keyboard_theme` (including dark/system modes), per-IM keyboard layout selection, `phonetic_keyboard_type`, and `enable_emoji_position` for inline emoji candidate placement, but no existing option to hide these soft-keyboard control buttons, freely reposition them, or make a selected generic keyboard layout automatically inherit/show the active IM's phonetic/table labels. Hermes posted routine acknowledgement `https://github.com/lime-ime/limeime/issues/90#issuecomment-4541351852` and forwarded the initial request for author-team/product evaluation. Reporter follow-up `https://github.com/lime-ime/limeime/issues/90#issuecomment-4541600221` clarified the desired product scope: closer-to-old UI, user-selectable placement/visibility for the English/number, Emoji, and voice buttons, keyboard-layout changes that still display the active IM's keys (for example phonetic labels), plus darker white-text color/theme combinations or system-theme-following behavior. No active bug watch, APK retest, or public duplicate reply needed unless Jeremy/maintainer chooses to product-plan it or the reporter adds implementation-specific evidence/questions.


- #91: Community-reported Android `.cin` import ordering bug from `ejmoog`: importing 哈哈倉頡 reports same-code `vmi` candidates changing from source order `狀 绒 戕` to `狀 戕 绒` even with `啟動選取排序` disabled. Classified as `bug` + `Usability`, assigned to `jrywu`, with analysis doc `docs/#91_ISSUE.md`. Public acknowledgement: `https://github.com/lime-ime/limeime/issues/91#issuecomment-4541903111`. Suspected area: Android `.cin` import / `LimeDB.getMappingByCode(...)` ordering, where imported rows get character-dependent `basescore` and fixed exact-match/single-character score priority can run before `_id ASC` even when user-facing sorting is off. Retest condition: ask reporter to retest only after a newer APK contains a targeted duplicate-code `.cin` ordering fix.


## Operational Handoff / Next Actions
- Time: 2026-05-23T02:55:00+08:00
- From: Hermes GitHub webhook (`push` to `master`, `2b60cbd` -> `9072bbb`).
- Summary: New Android pre-release APK `LIMEHD2026-6.1.11.apk` / version `6.1.11` arrived through `LimeStudio/app/release/output-metadata.json` in push commit `76d2da6c2687`. The push was initially missed because the Cloudflare tunnel connector was down; Hermes recovered the tunnel and processed the missed APK follow-up manually. Compared with 6.1.10, relevant Android-testable changes include #74 URL/search fields following normal remembered Chinese/English mode, #81 English-keyboard autocap behavior, #83 old Android settings UI removal, and cross-platform preference backup/restore. iOS #82 legacy iPhone globe-key support is present in source but not testable via the Android APK. Hermes posted #74 6.1.11 URL/search retest request `https://github.com/lime-ime/limeime/issues/74#issuecomment-4525986946` with the direct APK link.

- Needs: #74 no longer needs an active watch after SmithCCho's closure confirmation in comment `4527392181`; closed as completed with acknowledgement `https://github.com/lime-ime/limeime/issues/74#issuecomment-4527405069`; treat future URL/search preference changes as separate product scope if reopened/requested. Continue #72 table-source/license review and broader #72 product planning. Keep #76 closed unless the reporter/maintainer reopens or provides new evidence.

- Links: APK `https://raw.githubusercontent.com/lime-ime/limeime/master/LimeStudio/app/release/LIMEHD2026-6.1.11.apk`; #74 6.1.11 retest `https://github.com/lime-ime/limeime/issues/74#issuecomment-4525986946`; #74 issue `https://github.com/lime-ime/limeime/issues/74`; #74 reporter 6.1.10 numeric confirmation `https://github.com/lime-ime/limeime/issues/74#issuecomment-4523823506`; commits `aaed1a6db74d`, `dd5312ea9205`, `491ca1c616bb`, `76d2da6c2687`.


## Shared Task Board / Identity Exchange
- Purpose: This section is the repo-backed exchange area for Codex, Hermes, and future identities. Merge scheduled/background handoffs and task-board items here because local `.agents/shared/handoffs.md` and `.agents/shared/tasks.md` are not reliable for scheduled-session continuity.
- Format: `Status`, `Owner`, `Task`, `Context`, `Next`, `Updated`.
- Status: todo
  Owner: next LIME IME product/implementation identity
  Task: #72 Android 6.1.12 downloadable 哈哈倉頡 is reporter-confirmed and closed; #79 Android 6.1.12 visual/theme fix is reporter-confirmed, but the same comment asks whether emoji search should support Chinese IM code input. Keep #70/#71/#73/#74/#75/#76/#77/#78 closed/resolved unless reporters/maintainers follow up. #90 is a keyboard-interface customization enhancement/usability request acknowledged and forwarded for product evaluation; reporter follow-up clarified button visibility/placement, IM-aware layout labels, and darker/system-following theme requests.
  Context: #78 is maintainer-closed after commit `2e278c46` fixed optional-suggestion functional-key handling; the commit is now present on remote `master` in the 6.1.9 push, while manual iPhone simulator visual verification remains pending before TestFlight and is separate from the Android APK. #77 was maintainer-closed after commit `c828a2d2` addressed iOS `hasMoreMark` sentinel persistence in the candidate bar / expanded panel and a local simulator check verified the reported symptom; `docs/#77_ISSUE.md` records the resolved status. #70 asked to hide/configure next-code/extension-code candidates; maintainer `jrywu` suggested the suggested-candidate display-count option in comment `4506632626` and closed the issue, so it is no longer an active product-planning watch unless reopened or followed up by the reporter. #71 was a composition-cancel bug when switching Chinese to English; commit `3d5d9c5` includes the Android/iOS fix, reporter confirmed Android APK 6.1.8 fixed it in comment `4514378812`, and the issue is now closed/completed; #72 asked to add 哈哈倉頡; PR #84 is now merged to `master` as `fccf1fb64b6c91db25b744de2a09814e4d6d9940`, publishing `Database/cj4.limedb`, DB 104 support, Android/iOS catalog entries, and license attribution; #73 asked Enter to commit raw code while Space commits Chinese candidates and is now maintainer-closed as low-priority product consideration, with reporter follow-up `4517972425` noting Enter/Space still feel duplicated; #74 has analysis doc `docs/#74_ISSUE.md`; it remains primarily narrowed to URL/URI fields for remembered-mode behavior because users often type search keywords there, while email/phone/password should keep Android inputType-specific behavior; reporter comment `4508837104` separately asks whether number/date-time fields should use restricted LIME numeric/date-time layouts rather than English+number/symbol layout, especially for one-handed 行列10 input, and maintainer comment `4509415304` says this is acceptable if the field-specific numeric keyboard omits `中` and `abc` toggles ... [truncated]
  Next: For #70/#71/#73/#75/#77/#78, take no further action unless the reporter/maintainer reopens or comments with new evidence. For #90, no bug investigation or APK retest is needed; product-planning scope now includes configurable button visibility/placement, legacy/old-style layout options, selected layouts retaining active IM labels, and darker/system-following theme combinations. Resume only if Jeremy/maintainer wants implementation/product planning or the reporter adds implementation-specific evidence/questions. For #74, take no further action unless reopened or new evidence appears; 6.1.10 numeric/phone-field and 6.1.11 URL/search remembered-mode behavior are reporter-confirmed and the issue is closed. For #76, take no further action unless the reporter/maintainer reopens or comments with new evidence; 6.1.10 includes an additional `learn_phrase=false` runtime/LD phrase gate fix, but the learned-word/association/ranking disable/delete-control discussion remains historical product/usability context unless Jeremy/maintainer chooses to split/track it separately. For #78, no public retest request is needed; only resume if the iOS verification fails, the issue is reopened, or a tester-available release note is needed. For #72, take no further action unless reopened or new evidence appears; reporter confirmed Android APK `6.1.12` includes 哈哈倉頡 and the issue is closed as completed. For #79, reporter `SmithCCho` confirmed the Android APK `6.1.12` visual/theme fix in `https://github.com/lime-ime/limeime/issues/79#issuecomment-4531664505`; maintainer `jrywu` clarified in `https://github.com/lime-ime/limeime/issues/79#issuecomment-4531795839` that 6.1.12 supports Chinese emoji search while 6.1.11 and earlier do not. No automation public reply is needed now; watch for reporter follow-up after that clarification or maintainer closure decision. If the reporter still sees Chinese IM input failing on 6.1.12, distinguish version mismatch, Samsung/device/keyboard-mode path, direct composing-code limitation, or a separate bug/enhancement before posting again. iOS emoji-search/theme changes in commit `a763ee80b199360140185066f94ef619f4f2f716` still need normal iOS/TestFlight/manual verification separate from the Android APK.
  Updated: 2026-05-25T13:53:58+08:00 (CST)

## Historical Project-Memory Baseline
- Original automation/project memory said Android `6.1.1` was the latest known pre-release APK and gave this direct link: `https://raw.githubusercontent.com/lime-ime/limeime/master/LimeStudio/app/release/LIMEHD2026-6.1.1.apk`.
- Newer observed state supersedes that baseline where noted above: current observed pre-release APK is `6.1.12`; #55/#54/#62/#63/#64/#65/#66/#67/#68/#69/#70 are closed where noted; #63 was maintainer-closed as an upstream Xiaomi/Android recognizer limitation after LIME-side 6.1.6 mitigations and is no longer an active reminder/watch per Jeremy; #71 and #75 are reporter-confirmed fixed on 6.1.8 and closed; #72 is reporter-confirmed resolved/closed after Android APK 6.1.12 included the downloadable 哈哈倉頡 implementation; #74 is reporter-confirmed resolved/closed, #76 is reporter-confirmed fixed on 6.1.9 and closed for the #70 next-code candidate behavior, #78 is maintainer-closed after the optional-suggestion functional-key fix reached remote `master` in the 6.1.9 push, with iOS manual verification still pending; #77 is maintainer-closed after the `hasMoreMark` placeholder fix and local simulator verification, while #70 and #73 are maintainer-closed unless maintainer reopens them.

## Update Instructions
- Scheduled runs should update this file through GitHub API when current issue states, APK observations, run outcomes, cross-agent handoff/continuation notes, or shared task-board items change.
- Keep operational handoffs in `Operational Handoff / Next Actions` and shared task-board items in `Shared Task Board / Identity Exchange` so different identities can exchange state reliably.
- Do not store canonical rule/policy changes here; store those in the local automation memory file.
