# Automation Issue Context: Monitor lime-ime/limeime GitHub

Last updated: 2026-05-23T10:14:19+08:00 (CST)

## Source Of Rules
- Canonical role, communication style, issue-tracking policy, and APK/build policy are stored in the local automation memory file:
  `C:\Users\Jeremy\.codex\automations\monitor-lime-ime-limeime-github\memory.md`
- This repo file is for mutable issue-tracking context, operational handoffs, and shared task-board items only, so scheduled runs and different agent identities can read/update current state through GitHub API.

## Current Observed Repo State
- Latest GitHub Release observed: `v6.0.2` (published 2026-04-23).
- Android pre-release/build artifacts are not necessarily public GitHub Releases.
- Scheduled/API-only APK source of truth: `LimeStudio/app/release/output-metadata.json`.
- Last observed pre-release APK: `LIMEHD2026-6.1.10.apk` (versionName 6.1.10), metadata observed 2026-05-23T02:33:56+08:00; direct raw APK link `https://raw.githubusercontent.com/lime-ime/limeime/master/LimeStudio/app/release/LIMEHD2026-6.1.10.apk`. This APK includes Android #74 number/decimal restricted-keyboard routing, the Android/iOS #76 `learn_phrase=false` runtime phrase / LD phrase gate fix, and the broader iOS/iPad UI/layout, DB import/export, gesture-policy, and return-key adaptation changes on `master`.
- Raw APK URL pattern: `https://raw.githubusercontent.com/lime-ime/limeime/master/LimeStudio/app/release/<apk filename>`.
- No open pull requests were observed in the 2026-05-22T04:18:00+08:00 scheduled run.
- No GitHub Discussions tab was observed for the repository in recent runs.

## Current Issue States And Required Actions
- #54: Community-reported Brave URL bar candidate overlap/white-band behavior. Relevant fix landed in APK `LIMEHD2026-6.1.5.apk`; a retest request was posted and the issue is closed as completed. Remove from active watch unless reopened or referenced by new reports.
- #55: Community-reported key preview delay. Reporter confirmed improvement on 6.1.1 pre-release. Closed as completed; remove from active watch unless reopened or referenced by new reports.
- #58: `.lime` pipe-format import/support question. Automation replied in comment `4504038716` explaining `@format@|lime-text-v2` and escaping ASCII `|` as `\|`, while noting fullwidth/vertical `︱` should normally be direct data. Maintainer `jrywu` later pointed the reporter to `docs/CIN_LIME_SPEC.md#2-lime-format` in comment `4506696064`. Labels are `question` and `documentation`. Keep open for reporter follow-up or doc/product decision; if import still fails, ask for a minimal sample table. No new comments observed in the 2026-05-22T04:18:00+08:00 run.
- #59: `.lime` format/name-display documentation question. Maintainer answered key format/name details, reporter thanked them, and `jrywu` closed the issue on 2026-05-21. Treat as resolved/closed documentation question; no active bug watch or follow-up unless the reporter reopens or adds new evidence/question.
- #62: Community-reported Ext-B leading-character related-phrase issue. Reporter confirmed the 6.1.6 retest fixed/improved the remaining path; automation reacted and closed as completed. Remove from active watch unless reopened or referenced by new reports.
- #63: Community-reported Google voice input outputs Simplified Chinese and previously duplicated text. Reporter tested 6.1.6 after the voice-input routing/inline-dictation fixes and said it still outputs Simplified Chinese both with microphone permission enabled and disabled; clarification in comment `4501864655` says the normal Traditional Chinese output is from Sweetlime. Maintainer `jrywu` explained in comment `4509767537` that LIME already requests `zh-TW` and the system recognizer still returns Simplified Chinese on the reporter's Xiaomi setup; SweetLime uses legacy VoiceIME behavior that newer target API builds cannot reliably invoke. `jrywu` closed the issue on 2026-05-21 as effectively an upstream Xiaomi/Android recognizer limitation after LIME-side mitigations. Reporter follow-up in comment `4509924146` thanks the team and clarifies this is one Xiaomi phone with both LIME 6.1.6 and SweetLime installed, not two separate Xiaomi devices; no reopening request or new actionable LIME-side evidence yet. Treat as maintainer-closed; do not include #63 in routine reminder/watch summaries or shared task reminders unless the reporter reopens/adds new evidence, a clearly relevant platform workaround lands, or Jeremy explicitly asks to resume.
- #64: Community-reported settings text/inset/scroll issue. Reporter confirmed `6.1.2` fixed it; closed as completed. Remove from active watch unless reopened.
- #65: Maintainer-created Android table/assoc editor soft-keyboard sheet issue. Relevant fix landed in 6.1.4. Closed directly as completed; no tester invitation needed.
- #66: Maintainer-created iOS assoc/related editor score-field issue. Relevant fix landed in 6.1.4. Closed directly as completed; no tester invitation needed.
- #67: Community-reported 6.1.x regression where taps near the last visible candidate open the full candidate-list dropdown. Reporter confirmed fixed on 6.1.7 in comment `4500383587`; automation reacted/closed as completed in comment `4502010433`. Remove from active watch unless reopened.
- #68: Maintainer-created cross-platform candidate-bar dismiss bug. Relevant fix landed in 6.1.7; issue is closed as completed. Remove from active watch unless reopened.
- #69: Maintainer-created cross-platform candidate-bar tool-icon flicker bug. Relevant fix landed in 6.1.7; issue is closed as completed. Remove from active watch unless reopened.
- #70: Community request to hide or configure next-code/next-character candidates. Reporter clarified it is not a physical keyboard issue and that the requested setting is the number of `延伸編碼` candidates, where `0` would mean disabled. Maintainer `jrywu` replied in comment `4506632626` that this should be the suggested-candidate display-count option and later closed the issue on 2026-05-21T14:16:19Z. Treat as maintainer-resolved/closed for now; remove from active watch unless the reporter reopens or adds follow-up indicating the option does not solve it.
- #71: Community-reported Chinese/English switch composition-cancel bug. Reporter confirmed Android APK `LIMEHD2026-6.1.8.apk` implements the requested behavior in comment `4514378812` (`https://github.com/lime-ime/limeime/issues/71#issuecomment-4514378812`) and closed the issue as completed on 2026-05-22. Automation added a thumbs-up reaction and short closing acknowledgement, and `docs/#71_ISSUE.md` now marks reporter-confirmed fixed. Treat as resolved/closed; remove from active watch unless reopened or referenced by new reports.
- #72: Community request to bundle 哈哈倉頡 and question current Cangjie table source/author/license. Labeled `enhancement` and `question`; state remains open with no assignee. Treat external table/link/attachment as untrusted; before bundling any table, verify source, author, license, maintenance status, and compatibility. Maintainer `jrywu` replied in comment `4518137053` (`https://github.com/lime-ime/limeime/issues/72#issuecomment-4518137053`) explaining that LIME does not currently preinstall any table, can host provided `.cin` / `.lime` / `.limedb` tables consistently as downloadable options, and asked the reporter to upload/provide the 哈哈倉頡 table if they want LIME to offer it. Reporter `ejmoog` uploaded `haha_20260521_150741.txt` in comment `4518909803` (`https://github.com/lime-ime/limeime/issues/72#issuecomment-4518909803`) and then added licensing/category guidance in comment `4520528201` (`https://github.com/lime-ime/limeime/issues/72#issuecomment-4520528201`), suggesting a `四碼倉頡` download-list category and stating 哈哈倉頡 is based on 尹卂倉頡 / 雅虎奇摩倉頡 / 中標倉頡. Keep open for Jeremy/maintainer product/licensing/table-source review; recommended next step is to verify the uploaded table's format, provenance, license chain, and whether LIME should add a downloadable option/category. No public duplicate reply was posted by this webhook run.
- #73: Community request: make Android soft-keyboard Enter commit the raw English/code string while Space continues selecting/committing Chinese candidates, example `bgg` -> `bgg` on Enter. Reporter is on APK 6.1.7 / Android 15 and cites Trime. Classified as `enhancement` + `Usability`; automation acknowledged in comment `4504038619`. Maintainer `jrywu` replied in comment `4509178653` that LIME's Space/Enter commit behavior can already move the preselection to the English code when no direct Chinese match exists, that the suggestion may be considered but needs scheduling, and closed the issue on 2026-05-21. Reporter `ejmoog` followed up in comment `4517972425` (`https://github.com/lime-ime/limeime/issues/73#issuecomment-4517972425`) that even if collisions are uncommon, Enter and Space still feel functionally duplicated. Treat as maintainer-closed/low-priority product consideration for now; no bug investigation doc, public duplicate reply, or active watch is needed unless Jeremy/maintainer wants to reopen/product-plan it or the reporter adds new actionable evidence.
- #74: Community question/enhancement about Android `記憶中英模式` and inputType-driven switching. Reporter asks whether the option should make LIME always start in Chinese mode and requests an optional fixed-Chinese-start override for 行列10 numeric-key Chinese input even in URL/number fields. Labels are `question` + `enhancement` + `Usability`. Maintainer narrowed direction: URL/URI currently stays on the IM-family English layout with a `中` switch path, while number/phone restricted fields should use a phone/numeric keyboard without `中` / `abc` toggles. Commit `13c888e84bcf` refined Android restricted keyboard layouts and iOS return-key handling; Android APK `LIMEHD2026-6.1.10.apk` contains the #74 number/decimal restricted-keyboard routing. Hermes posted retest request `https://github.com/lime-ime/limeime/issues/74#issuecomment-4521770421`; reporter `SmithCCho` replied in `https://github.com/lime-ime/limeime/issues/74#issuecomment-4523823506` that number/decimal/phone fields now only show the LIME phone-number keyboard, so that scoped APK fix is reporter-confirmed. The same reply asks again to make URL/address-bar fields keep Chinese input by default because 行列10 cannot mix Chinese/English as easily as 倉頡/大星 and browser address bars are often used for keyword search. Issue remains open for the unresolved URL/URI remembered-mode product decision; do not post a duplicate public reply until Jeremy/maintainer decides whether to keep current URL behavior or reopen URL-as-normal-text planning. Analysis doc `docs/#74_ISSUE.md` records the reporter confirmation and remaining URL decision.

- #75: Community-reported Android keyboard redraw/popup-lifecycle bugs on 6.1.7 / Android 15. Fix commit `9230916` landed and APK `LIMEHD2026-6.1.8.apk` contains the relevant keyboard popup/redraw changes. Retest request was posted in `https://github.com/lime-ime/limeime/issues/75#issuecomment-4510656707`; reporter `ejmoog` confirmed in `https://github.com/lime-ime/limeime/issues/75#issuecomment-4514371698` that installing 6.1.8 resolved the issue, then closed it. Automation added a thumbs-up reaction and a short closing acknowledgement. Treat as resolved/closed; remove from active watch unless reopened or referenced by new reports.
- #76: Community-reported Android next-code/extension candidate display bug split from #70 after reporter tested APK `LIMEHD2026-6.1.8.apk` and said the extra candidate still appeared. Fix commit `7e1d57b` is on `master`; Android APK `LIMEHD2026-6.1.9.apk` contained the targeted `建議字顯示數量 = 0` / exact-match-only fix. Reporter `ejmoog` confirmed in `https://github.com/lime-ime/limeime/issues/76#issuecomment-4520021201` that the scoped problem was resolved, then closed the issue; Hermes acknowledged in `https://github.com/lime-ime/limeime/issues/76#issuecomment-4520032322`. Later commit `5d66014dead7` in APK `LIMEHD2026-6.1.10.apk` fixes the adjacent `learn_phrase=false` ungated runtime phrase / LD phrase learning paths on Android and iOS, but #76 remains reporter-closed for the scoped exact-match issue. Do not post another #76 retest prompt or reopen unless the reporter/maintainer asks or new evidence appears; if learned-word/association/ranking-control concerns resurface, distinguish existing learned/table entries, relation candidates, and selection-ranking data before deciding whether to split or triage separately.


- #77: Maintainer-created iOS second-stage candidate placeholder bug. Maintainer `jrywu` posted fix summary in comment `4517540556` and closed the issue on 2026-05-22 after local simulator verification; commit `c828a2d2` is referenced by GitHub. Analysis doc `docs/#77_ISSUE.md` now records the closed/fixed status. Treat as resolved/closed; no public acknowledgement, reporter retest request, or active watch is needed unless new iOS evidence appears.
- #79: Community usability request from `SmithCCho`: in dark mode, the Android emoji panel search box remains too bright/white and should use a gray/black themed background to avoid glare. Classified as `enhancement` + `Usability`; Hermes acknowledged in comment `https://github.com/lime-ime/limeime/issues/79#issuecomment-4523853744` and did not create a bug analysis doc because this is a visual/theme enhancement request rather than a confirmed regression. Keep open for author-team UI/theme evaluation; likely implementation scope is making the emoji search field background plus text/hint colors follow the active dark keyboard/emoji-panel theme.


## Operational Handoff / Next Actions
- Time: 2026-05-23T02:55:00+08:00
- From: Hermes GitHub webhook (`push` to `master`, `2b60cbd` -> `9072bbb`).
- Summary: New Android pre-release APK `LIMEHD2026-6.1.10.apk` / version `6.1.10` arrived through `LimeStudio/app/release/output-metadata.json`. Compared commit range includes #74 restricted number/decimal keyboard routing and #76 ungated `learn_phrase=false` runtime/LD phrase learning fixes, plus iOS/iPad UI/layout, DB, gesture-policy, and return-key adaptation work. Hermes posted #74 retest request `https://github.com/lime-ime/limeime/issues/74#issuecomment-4521770421` with the direct APK link. No #76 public follow-up was posted because #76 is already reporter-confirmed closed for the scoped 6.1.9 exact-match issue and the learned-word/association concern is not an active watch unless reopened/new evidence appears.
- Needs: Watch #74 for reporter confirmation or negative retest. If #74 is confirmed fixed, update `docs/#74_ISSUE.md`, close/acknowledge if appropriate under policy, and remove the watch. If #76 resurfaces, treat the 6.1.10 `learn_phrase=false` fix as relevant context but do not overclaim it verifies existing learned/table entries or ranking-control behavior. Continue #72 table-source/license review and broader #74/#72 product planning.
- Links: APK `https://raw.githubusercontent.com/lime-ime/limeime/master/LimeStudio/app/release/LIMEHD2026-6.1.10.apk`; #74 retest `https://github.com/lime-ime/limeime/issues/74#issuecomment-4521770421`; #74 issue `https://github.com/lime-ime/limeime/issues/74`; #76 issue `https://github.com/lime-ime/limeime/issues/76`; #76 confirmation `https://github.com/lime-ime/limeime/issues/76#issuecomment-4520021201`; commits `13c888e84bcf`, `5d66014dead7`, `e0a5c38bc75e`.

## Shared Task Board / Identity Exchange
- Purpose: This section is the repo-backed exchange area for Codex, Hermes, and future identities. Merge scheduled/background handoffs and task-board items here because local `.agents/shared/handoffs.md` and `.agents/shared/tasks.md` are not reliable for scheduled-session continuity.
- Format: `Status`, `Owner`, `Task`, `Context`, `Next`, `Updated`.
- Status: todo
  Owner: next LIME IME product/implementation identity
  Task: Review/plan #72/#74/#79 enhancement requests and watch #74 6.1.10 retest; keep #70/#71/#73/#75/#76/#77/#78 closed unless reporters/maintainers follow up.
  Context: #78 is maintainer-closed after commit `2e278c46` fixed optional-suggestion functional-key handling; the commit is now present on remote `master` in the 6.1.9 push, while manual iPhone simulator visual verification remains pending before TestFlight and is separate from the Android APK. #77 was maintainer-closed after commit `c828a2d2` addressed iOS `hasMoreMark` sentinel persistence in the candidate bar / expanded panel and a local simulator check verified the reported symptom; `docs/#77_ISSUE.md` records the resolved status. #70 asked to hide/configure next-code/extension-code candidates; maintainer `jrywu` suggested the suggested-candidate display-count option in comment `4506632626` and closed the issue, so it is no longer an active product-planning watch unless reopened or followed up by the reporter. #71 was a composition-cancel bug when switching Chinese to English; commit `3d5d9c5` includes the Android/iOS fix, reporter confirmed Android APK 6.1.8 fixed it in comment `4514378812`, and the issue is now closed/completed; #72 asks to bundle 哈哈倉頡 and raises source/license questions, and reporter `ejmoog` has now uploaded `haha_20260521_150741.txt` for consideration; #73 asked Enter to commit raw code while Space commits Chinese candidates and is now maintainer-closed as low-priority product consideration, with reporter follow-up `4517972425` noting Enter/Space still feel duplicated; #74 has analysis doc `docs/#74_ISSUE.md`; it remains primarily narrowed to URL/URI fields for remembered-mode behavior because users often type search keywords there, while email/phone/password should keep Android inputType-specific behavior; reporter comment `4508837104` separately asks whether number/date-time fields should use restricted LIME numeric/date-time layouts rather than English+number/symbol layout, especially for one-handed 行列10 input, and maintainer comment `4509415304` says this is acceptable if the field-specific numeric keyboard omits `中` and `abc` toggles ... [truncated]
  Next: For #70/#71/#73/#75/#77/#78, take no further action unless the reporter/maintainer reopens or comments with new evidence. For #74, watch the 6.1.10 retest request and update/close only after reporter confirmation or maintainer instruction. For #76, take no further action unless the reporter/maintainer reopens or comments with new evidence; 6.1.10 includes an additional `learn_phrase=false` runtime/LD phrase gate fix, but the learned-word/association/ranking disable/delete-control discussion remains historical product/usability context unless Jeremy/maintainer chooses to split/track it separately. For #78, no public retest request is needed; only resume if the iOS verification fails, the issue is reopened, or a tester-available release note is needed. Plan #74 implementation for URL/URI remembered-mode behavior; evaluate #79 emoji search-field dark-theme styling and number/date-time/phone numeric-keyboard behavior without `中`/`abc` toggles where the field disallows English/Chinese input; review #72's uploaded 哈哈倉頡 table/source/license/maintenance status before any downloadable-list decision; do not bundle or host external tables without source/author/license verification.
  Updated: 2026-05-23T02:55:00+08:00
- Status: todo
  Owner: next LIME IME documentation/import identity
  Task: Watch #58 `.lime` v2 escaping follow-up.
  Context: Automation answered how to import ASCII `|` with `@format@|lime-text-v2` and `\|`; maintainer `jrywu` also pointed the reporter to `docs/CIN_LIME_SPEC.md#2-lime-format` in comment `4506696064`. Reporter may still provide a sample table if import fails.
  Next: If reporter shares a failing sample, inspect importer/docs and update docs or bug investigation as appropriate. No additional public reply is needed unless the reporter remains confused or provides new evidence.
  Updated: 2026-05-22T04:18:00+08:00

## Historical Project-Memory Baseline
- Original automation/project memory said Android `6.1.1` was the latest known pre-release APK and gave this direct link: `https://raw.githubusercontent.com/lime-ime/limeime/master/LimeStudio/app/release/LIMEHD2026-6.1.1.apk`.
- Newer observed state supersedes that baseline where noted above: current observed pre-release APK is `6.1.10`; #55/#54/#62/#63/#64/#65/#66/#67/#68/#69/#70 are closed where noted; #63 was maintainer-closed as an upstream Xiaomi/Android recognizer limitation after LIME-side 6.1.6 mitigations and is no longer an active reminder/watch per Jeremy; #71 and #75 are reporter-confirmed fixed on 6.1.8 and closed; #72/#74 remain open enhancement/question/product-decision items, #76 is reporter-confirmed fixed on 6.1.9 and closed for the #70 next-code candidate behavior, #78 is maintainer-closed after the optional-suggestion functional-key fix reached remote `master` in the 6.1.9 push, with iOS manual verification still pending; #77 is maintainer-closed after the `hasMoreMark` placeholder fix and local simulator verification, while #70 and #73 are maintainer-closed unless maintainer reopens them.

## Update Instructions
- Scheduled runs should update this file through GitHub API when current issue states, APK observations, run outcomes, cross-agent handoff/continuation notes, or shared task-board items change.
- Keep operational handoffs in `Operational Handoff / Next Actions` and shared task-board items in `Shared Task Board / Identity Exchange` so different identities can exchange state reliably.
- Do not store canonical rule/policy changes here; store those in the local automation memory file.